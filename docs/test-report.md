# 測試報告

- 日期：2026-09-11
- 版本：1.0.0（commit 見儲存庫紀錄）
- 環境：macOS（Darwin 24.0.0）、Node.js v25.9.0；瀏覽器驗證使用 Chromium 內嵌瀏覽器，本機 `python3 -m http.server`

## 1. 單元與整合測試（`npm test`）

結果：**55 項通過，0 項失敗**（約 0.4 秒）。

| 檔案 | 測試數 | 涵蓋驗收條件 |
|---|---|---|
| tests/parser.test.js | 9 | AC-03、AC-08：優先序、白名單函數、錯誤位置、拒絕任意程式碼、除以零／非有限值、預覽共用 AST |
| tests/units.test.js | 7 | AC-08：預設式單位一致、不一致指出位置、常數項／非常數指數明示未驗證 |
| tests/compile.test.js | 5 | AC-03：未知符號位置、循環衍生式、缺少必要公式、語法錯誤 |
| tests/solver.test.js | 11 | AC-04：獨立求解找回校準點、無解／無唯一解／病態、負價格與負分群需求不適用、Newton、找不到解、單市場模式 |
| tests/model.test.js | 8 | AC-02、AC-07、AC-08、AC-11：無 min/max、單調性、各因素移線方向符合第 03 頁、產能／預算右移、r 共用、百分數不除 100、來源標記 |
| tests/scenario.test.js | 6 | AC-06、AC-12：JSON 往返（公式、輸入、結果、來源標記）、缺值不補 0、基準 0 百分比不適用、輸入驗證 |
| tests/session.test.js | 7 | AC-02、AC-03：公式修改重算、錯誤保留最後有效結果、除以零位置、重設／儲存基準、複製情境、單市場 |
| tests/charts-consistency.test.js | 2 | AC-05：曲線在均衡價交於聯立結果、基準／新曲線各用自己的跨市場價格、取樣不裁切負值 |

完整輸出（spec reporter）：

```
✔ R05：各圖曲線在均衡價交於聯立結果；基準／新曲線各用自己的跨市場價格
✔ 曲線取樣不裁切負值
✔ 預設公式可編譯並正確排序衍生式
✔ 未知符號指出公式與位置
✔ 循環衍生式
✔ 缺少必要公式、重新定義因素
✔ 語法錯誤帶公式 id
✔ R07：預設式不含 min／max，也沒有 T、kappa_H、Q_C_max
✔ 固定跨市場價格時：供給對自身價格非遞減、需求非遞增
✔ 各因素局部移線方向符合第 03 頁；已吸收效果顯示為無直接位移
✔ 產能、容量、存量、預算增加 → 對應曲線右移
✔ 跨市場價格傳導方向（第 04 頁 §2）
✔ R02：調利率時三個市場共用同一 r 並重新聯立求解
✔ 百分數以百分數值運算：r=3.64 直接代入
✔ R08：示範值具來源標記；觀測值只有 r、e
✔ 運算子優先序與結合性
✔ 符號與白名單函數
✔ 全形／數學符號別名
✔ 語法錯誤附位置
✔ 不允許任意程式碼
✔ 函數參數個數
✔ 除以零與非有限值指出位置
✔ 公式預覽使用同一語法樹
✔ containsCall 偵測 min/max
✔ JSON 匯出再匯入：公式、輸入、結果、來源標記皆重現
✔ 匯出檔包含單位與求解設定
✔ 匯入缺值（待填、null）時報錯，不補 0
✔ 匯入錯誤 schema、無效來源標記
✔ 基準為 0 時百分比變化不適用
✔ 輸入驗證：h>0、u≥1、成功率 0–100、非負；利率可為負
✔ 修改一條有效公式 → 重新解析、求解
✔ 錯誤公式 → 具體訊息，保留最後有效圖表並標示過期
✔ 除以零 → 公式錯誤並指出位置
✔ 輸入錯誤（h=0）→ 暫停求解
✔ 重設回復基準；儲存為基準；係數重設
✔ 複製與載入情境
✔ 單市場模式：固定價來自基準均衡，結果標示 single
✔ 示範情境：獨立求解流程找回校準點（不提供參考價格）
✔ 示範情境：以參考價格尺度化時殘差合格
✔ 線性系統：無解、無唯一解、病態
✔ 奇異聯立式：硬體價格項全為 0 → 無解
✔ 奇異聯立式：供需恆等 → 無唯一解
✔ 負價格 → 不在適用範圍，不截斷
✔ 負分群需求 → 不在適用範圍
✔ 非線性公式使用 Newton 法
✔ 非線性且無解 → 找不到解，不沿用舊解
✔ 單市場模式：其他兩價固定
✔ 單市場模式：非線性公式
✔ 所有符號與係數單位可解析
✔ 預設公式全部單位一致
✔ 加減不同單位 → 不一致並指出位置
✔ 結果單位與宣告不同 → 不一致
✔ 常數項或非常數指數 → 明示未驗證
✔ exp 參數須無單位
✔ PFLOP-hour 顯示
ℹ tests 55
ℹ pass 55
ℹ fail 0
```

## 2. 瀏覽器驗證（本機）

| # | 步驟 | 結果 |
|---|---|---|
| B1 | 1440×900 開啟首頁 | ✅ 左側面板＋三圖並排；狀態「有效均衡」，線性求解，最大相對殘差 0，尺度化條件數 1.82，10 條公式單位一致；主控台無錯誤 |
| B2 | 因素面板把 r 改為 6 | ✅ 三市場同時重算：P_H 9000→8769、P_C 0.4→0.3889、P_A 2→2.009；r 標記變為「使用者輸入」；仍為有效均衡 |
| B3 | Q_H^D 末尾加上未知符號 `zzz` | ✅ 狀態「公式錯誤」並指出「未知符號 zzz（第 68 個字元）」，編輯器標出位置；三張圖標示「最後有效結果，不是新均衡」 |
| B4 | 重新整理頁面 | ✅ r＝6 與修改後的公式從本機儲存恢復 |
| B5 | 按「重設」 | ✅ r 回到 3.64（來源觀測值）、公式回到預設、有效均衡 |
| B6 | Q_H^D 末尾加上 `+ 2e7` | ✅ 重新求解：P_H 9000→9532（+5.91%）、Q_H +5.91%；單位狀態「未驗證：常數 2e7 沒有單位」；預覽顯示 2×10⁷ |
| B7 | 切換「單市場觀察」（硬體） | ✅ 只畫硬體圖＋說明卡；狀態與表格標示「其他市場價格固定，條件均衡」；P_H 9000→9500 |
| B8 | 375×812 手機尺寸 | ✅ 單欄堆疊，文件寬度 375px 無水平捲動，圖表寬 355px 可讀 |

測試中發現並已修正的問題：

1. Newton 法在價格發散時相對殘差變小而誤判收斂 → 改為同時檢查起點量級（`solver.js`）。
2. 非線性項在測試點量級極小時被判為線性，代回核對失敗 → 改為退回 Newton 法，找不到則回報「找不到解」。
3. CSS `display` 覆蓋 `hidden` 屬性，造成空白通知框 → 加入 `[hidden]{display:none!important}`。
4. 座標軸刻度顯示 `5.00e+3` → 改為一般數字寫法。

未在瀏覽器實測、改由單元測試涵蓋的項目：JSON 匯出下載與檔案匯入的操作流程（往返一致性由 `scenario.test.js` 驗證）、深色模式外觀、純鍵盤完整走查（分頁方向鍵、圖表方向鍵已實作）。

## 3. 部署驗證（正式網址）

- 正式網址：https://perry-666.github.io/ai-three-market-simulator/
- 儲存庫：https://github.com/Perry-666/ai-three-market-simulator
- 驗證版本：commit `599f7db`（GitHub Pages，HTTPS，`https_enforced: true`）
- 訪客環境：內嵌 Chromium 瀏覽器，未登入 GitHub、無既有本機儲存；第二工作階段為另一個獨立的 Chrome 瀏覽器

| # | 清單項目 | 結果 |
|---|---|---|
| D1 | 未登入開啟正式網址 | ✅ HTTP 200；三張圖、「有效均衡」；主控台無錯誤 |
| D2 | 調整利率 r＝6 | ✅ 三市場同時重算：P_H −2.57%、P_C −2.77%、P_A +0.46% |
| D3 | 修改公式：Q_H^D 加上 `/ (r - r)` | ✅ 「除以零：「(r - r)」的值為 0（第 68 個字元）」，三圖標示最後有效結果 |
| D4 | 修改公式：Q_H^D 加上 `+ 2e7` | ✅ 重算為有效均衡，P_H 9000→9532（+5.91%），單位狀態「未驗證」 |
| D5 | 重新整理 | ✅ r＝6 與修改後公式從本機儲存恢復 |
| D6 | 重設 | ✅ r 回到 3.64、公式回到預設、有效均衡 |
| D7 | 兩個獨立工作階段 | ✅（單向）工作階段 A 已保存 r＝6 後，另一瀏覽器 B 重新整理仍為預設 r＝3.64，未受 A 影響。B 端輸入因自動化工具無法把鍵盤輸入送進該 Chrome 視窗而未完成，反方向未實測；兩者各自使用瀏覽器本機儲存，無共用後端 |
| D8 | JSON 匯出／匯入 | ⚠ 未在正式網址操作（下載與選檔需使用者介面操作）；往返一致性由 `scenario.test.js` 驗證 |
| D9 | 手機寬度 | ⚠ 未在正式網址重測；本機 375×812 已驗證（B8），部署內容為相同靜態檔 |

部署驗證期間修正：重算排程由 `requestAnimationFrame` 改為計時器（commit `599f7db`），避免背景分頁暫停動畫影格時延遲重算；修正後 D4、D6 於正式網址重新驗證通過。
